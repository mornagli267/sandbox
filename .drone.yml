kind: pipeline
type: docker
name: default

steps:
  - name: print-start
    image: alpine
    commands:
      - echo "Start Buuldda:"
  - name: print-end
    image: alpine
    commands:
      - echo "End Build."
