metadata:
  format: Lava-Test Test Definition 1.0
  name: Redfish Power Off
  description: Send ForceOff command via Redfish API

run:
  steps:
    - apt-get update -qq
    - apt-get install -y curl
    - cd redfish/scripts
    - bash power_off.sh